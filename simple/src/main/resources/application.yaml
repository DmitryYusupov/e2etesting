application:
  host: localhost
  port: 8080

server:
  port: 8081

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/javaquiz?currentSchema=e2etesting
    username: test
    password: 111